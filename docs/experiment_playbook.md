# Experiment Playbook

- Prepare a dataset into `data/processed/`.
- Generate traces on the source split.
- Distill skills from those traces.
- Build a library snapshot.
- Run `direct` and `trs` on the same holdout.
- Compare JSON summaries and Markdown reports.
